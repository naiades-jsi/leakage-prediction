# leakage-prediction
Algorithm for locating and isolating leakages in water distribution networks. Current work for Braila.


### Usage
python3 main.py [-t -r -x]

- -t Testing mode, will not send data back and will discard all changes
- -r Resets the service back to the start
- -x Force execute - will start the service even with trigger off